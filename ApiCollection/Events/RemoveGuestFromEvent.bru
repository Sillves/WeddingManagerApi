meta {
  name: RemoveGuestFromEvent
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/events/{{eventId}}/guests/{{guestId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
