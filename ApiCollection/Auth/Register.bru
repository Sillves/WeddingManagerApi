meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/register
  body: none
  auth: inherit
}

vars:pre-request {
  baseUrl: http://localhost:5072
}

settings {
  encodeUrl: true
  timeout: 0
}
